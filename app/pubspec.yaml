name: camtest
description: DESKMATE Raspberry Pi 5 Atlas 자세 화면.
publish_to: none
version: 0.2.0+1

environment:
  # Atlas Flutter 3.27.4 가 쓰는 Dart 는 3.6.x 다. 옆 앱(display/atlas/app)과 맞춘다.
  sdk: ^3.6.2

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
